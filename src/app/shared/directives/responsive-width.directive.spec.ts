import { ResponsiveWidthDirective } from './responsive-width.directive';

describe('ResponsiveWidthDirective', () => {
  it('should create an instance', () => {
    const directive = new ResponsiveWidthDirective();
    expect(directive).toBeTruthy();
  });
});
